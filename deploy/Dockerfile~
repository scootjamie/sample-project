FROM node:10

ENV NODE_ENV=production

WORKDIR /var/bugreporter

# Install dependencies
COPY package.json yarn.lock ./

RUN yarn install --no-cache --production && yarn cache clean

# Copy rest of app
COPY . .

CMD [ "yarn", "start" ]
